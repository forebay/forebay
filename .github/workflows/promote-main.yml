name: Promote to Main
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'
permissions:
  contents: write
jobs:
  promote:
    uses: intisy/workflows/.github/workflows/merge.yml@main
    with:
      source: development
      target: main
      sync_back: true
